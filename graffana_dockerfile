# Use official Grafana image
FROM grafana/grafana:latest

# Set the environment variable for admin password
ENV GF_SECURITY_ADMIN_PASSWORD=your_password

# Expose Grafana's default port
EXPOSE 3000

# Grafana is automatically configured to start when the container starts
